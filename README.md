# Bookmarks

This will become **bookmarks.seangeng.com** — a searchable personal X bookmarks library with vector search and topic grouping.

## Seed data

Seed data lives in [`data/bookmarks-seed.json`](data/bookmarks-seed.json) (~196 bookmarks from [@seangeng](https://x.com/seangeng)).

Also see [`data/prune-stats.json`](data/prune-stats.json) for the link prune report summary.
